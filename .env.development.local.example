# Development-only overrides. Next loads .env.development.local only in
# development and it wins there, so `npm run dev` targets localhost while
# .env.local holds the production values the deploy scripts read.
APP_URL=http://localhost:3000
